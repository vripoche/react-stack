language: node_js
node_js:
  - "8.10"
script:
  - npm run lint
  - npm test
install:
  - yarn install
before_deploy:
  - npm run build
deploy:
  provider: pages
  github-token: $GITHUB_TOKEN
  skip-cleanup: true
  local-dir: dist
  on:
    branch: master
